# Project-local tool categories

Consider supporting `toolctl.py create-category <name>` to create dynamically
discoverable category entry points in the project repository instead of the
Agent Framework submodule.
