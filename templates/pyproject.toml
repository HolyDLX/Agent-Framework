[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "${distribution_name}"
version = "0.1.0"
description = ${intent_toml}
requires-python = ">=${python_version}"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
