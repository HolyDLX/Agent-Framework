# Architecture

## Initial intent

${intent}

## Architecture baseline

The initial intent is the only project-specific architecture input established
during bootstrap. Define system boundaries, responsibilities, public/internal
boundaries, dependencies, constraints, and unresolved architecture decisions
here as they become known. Do not invent detail merely to fill this document.
